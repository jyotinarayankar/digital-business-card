/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/digital-business-card',
  assetPrefix: '/digital-business-card',
}

export default nextConfig;

